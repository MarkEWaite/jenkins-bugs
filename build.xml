<project name="JENKINS-43198" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <echo>java is ${java.version}</echo>
    <buildnumber/>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="${user.name} buildnumber++, was ${build.number}"/>
        <arg value="build.number"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>

    <property environment="env"/>
    <property name="env.BUILD_NUMBER" value="42"/> <!-- in case not set in env -->

    <mkdir dir="this_is_ok/more/subdirs"/>
    <echo file="this_is_ok/more/subdirs/build.number">build.number=123${env.BUILD_NUMBER}</echo>
    <echo>this_is_ok wrote 123${env.BUILD_NUMBER}</echo>

    <mkdir dir="this_is/not_ok/more/subdirs"/>
    <echo file="this_is/not_ok/more/subdirs/build.number">build.number=456${env.BUILD_NUMBER}</echo>
    <buildnumber file="this_is/not_ok/more/subdirs/build.number"/>
    <echo>not_ok wrote 456${env.BUILD_NUMBER}, previously was ${build.number}</echo>

    <concat>
      <filelist dir="this_is/not_ok/more/subdirs" files="build.number"/>
    </concat>

  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
