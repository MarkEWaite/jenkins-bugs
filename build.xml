<project name="JENKINS-22547" default="increment" basedir=".">

  <!-- Report info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="status" />
  </target>

  <!-- Increment build number, commit the change -->
  <target name="increment" description="Submit a change to show JENKINS-22547 is fixed">
    <exec executable="git">
      <arg value="pull"/>
    </exec>
    <buildnumber/>
    <echo>Build number was ${build.number}</echo>
    <exec executable="git">
      <arg value="add"/>
      <arg value="build.number"/>
    </exec>
    <exec executable="git">
      <arg value="commit"/>
      <arg value="-m"/>
      <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
    </exec>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
