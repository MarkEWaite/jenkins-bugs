<project name="JENKINS-53346" default="increment" basedir=".">

  <property environment="env"/>

  <target name="is-first-build">
    <condition property="first.build">
      <equals arg1="${env.BUILD_NUMBER}" arg2="1"/>
    </condition>
  </target>

  <!-- Report environment variables only in builds 2+ -->
  <target name="check-non-initial-build" description="Check git env vars at build 2+" depends="is-first-build" if="env.JENKINS_URL" unless="first.build">
    <echo>GIT_PREVIOUS_COMMIT=${env.GIT_PREVIOUS_COMMIT}</echo>
    <!-- <echo>GIT_PREVIOUS_SUCCESSFUL_COMMIT=${env.GIT_PREVIOUS_SUCCESSFUL_COMMIT}</echo> -->
  </target>

  <!-- Report environment variables in all builds -->
  <target name="report-git-env-vars" description="Check all git env vars" depends="check-non-initial-build" if="env.JENKINS_URL">
    <echo>GIT_AUTHOR_EMAIL=${env.GIT_AUTHOR_EMAIL}</echo>
    <echo>GIT_AUTHOR_NAME=${env.GIT_AUTHOR_NAME}</echo>
    <echo>GIT_BRANCH=${env.GIT_BRANCH}</echo>
    <echo>GIT_COMMIT=${env.GIT_COMMIT}</echo>
    <echo>GIT_COMMITTER_EMAIL=${env.GIT_COMMITTER_EMAIL}</echo>
    <echo>GIT_COMMITTER_NAME=${env.GIT_COMMITTER_NAME}</echo>
    <echo>GIT_LOCAL_BRANCH=${env.GIT_LOCAL_BRANCH}</echo>
    <echo>GIT_URL=${env.GIT_URL}</echo>
  </target>

  <target name="increment" description="Increment build number">
    <buildnumber/>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
        <arg value="build.number"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo" depends="report-git-env-vars">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <!-- JENKINS-53346 reports that checkout returns the same map on a second
         call even though the branch for checkout is different than first. -->
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
