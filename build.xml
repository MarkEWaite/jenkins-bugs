<project name="JENKINS-34309" default="push" basedir=".">

  <!-- Synchronize repositories -->
  <target name="push" description="Push a change with Ctrl-M in the message to this branch">
    <echo>java is ${java.version}</echo>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="Message contains Ctrl-M in the middle of line 1"/>
      </args>
    </git>
  </target>

  <!-- Report info about this repo -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="3"/>
      </args>
    </git>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
