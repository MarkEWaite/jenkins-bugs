<project name="JENKINS-12545" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <buildnumber/>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] build++, was ${build.number}"/>
        <arg value="build.number"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <!-- JENKINS-22795 reports that files created before clone are removed by clone -->
    <loadfile property="file.content" srcFile="pre-checkout-file.txt" failonerror="false"/>
    <property name="file.content" value="[missing]"/>
    <echo>File content for pre-checkout-file.txt is ${file.content}</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>

<!-- Sample bug ID's to rotate through this project -->
<!-- JENKINS-12545 JENKINS-14798 JENKINS-22795 JENKINS-29853 JENKINS-31108 JENKINS-32174 -->
