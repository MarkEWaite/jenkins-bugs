<project name="JENKINS-56063-refspec-env-reference-not-expanded" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
      </args>
    </git>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <!-- JENKINS-56063 refspec env reference expanded by shell, not expanded by Jenkins -->
    <!-- Report remote.origin.fetch -->
    <!-- Does not show what expanded it, only that it has correct value -->
    <echo>Reporting remote.origin.fetch value</echo>
    <git command="config">
      <args>
        <arg value="--get"/>
        <arg value="remote.origin.fetch"/>
      </args>
    </git>
    <echo>End of reporting remote.origin.fetch value</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
