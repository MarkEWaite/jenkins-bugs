<project name="JENKINS-60591" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
      </args>
    </git>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="First build++, [${ant.project.name}] ${user.name} was ${build.number}"/>
      </args>
    </git>
    <git command="push"/>
    <git command="push">
      <args>
        <arg value="cache"/>
      </args>
    </git>
    <echo>sleeping 31 seconds</echo>
    <sleep seconds="31"/> <!-- Wait long enough for job to start but not finish -->
    <echo>slept 31 seconds, awake and continuing</echo>
    <ant antfile="build.xml" dir="." target="increment-two" inheritAll="false"/>
  </target>

  <!-- Increment without sleep - called from first build script -->
  <target name="increment-two" description="Increment build number">
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="Second build++, [${ant.project.name}] ${user.name} was ${build.number}"/>
      </args>
    </git>
    <git command="push"/>
    <git command="push">
      <args>
        <arg value="cache"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo without sleeping">
    <exec executable="git" outputproperty="git.HEAD">
      <arg value="rev-parse"/>
      <arg value="HEAD"/>
    </exec>
    <echo>Checkout has git HEAD ${git.HEAD}</echo>
    <echo>user dir is ${user.dir}</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
