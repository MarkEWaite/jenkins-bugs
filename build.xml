<project name="JENKINS-68751" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <buildnumber/>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="${ant.project.name} ${user.name} number++, was ${build.number}"/>
        <arg value="build.number"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <!-- JENKINS-68751 reports non-global ssh key credentials scoped credentials are not used in checkout -->
    <exec executable="./reportScanLogResults" failonerror="true"/>
    <echo>reportScanLogResults script exited normally</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}" failonerror="true">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
