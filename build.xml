<project name="JENKINS-36507" default="info" basedir=".">

  <!-- Report all branch names as info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch">
      <args>
        <arg value="-a"/>
      </args>
    </git>
  </target>

</project>
