<project name="JENKINS-58049" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
        <arg value="--prune"/>
      </args>
    </git>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <!-- JENKINS-58049 reports sparse checkout does not support multiple paths -->
    <fileset id="files.contents" dir="." includes="C*"/>
    <property name="prop.files.contents" refid="files.contents"/>
    <echo>Files contents for 'C*' are ${prop.files.contents}</echo>
    <dirset id="dir.contents" dir="." includes="C*"/>
    <property name="prop.dir.contents" refid="dir.contents"/>
    <echo>Dir contents for 'C*' are ${prop.dir.contents}</echo>
    <git command="remote">
      <args>
        <arg value="-v"/>
      </args>
    </git>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
