<project name="JENKINS-58503" default="increment" basedir=".">

  <property environment="env"/>

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
      </args>
    </git>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>env.BRANCH is ${env.BRANCH}</echo>
    <echo>env.GIT_BRANCH is ${env.GIT_BRANCH}</echo>
    <echo>env.GIT_COMMIT is ${env.GIT_COMMIT}</echo>
    <echo>java is ${java.version}</echo>
    <!-- JENKINS-58503 provides examples of the git checkout step using symbols. -->
    <echo>user dir is ${user.dir}</echo>
    <!-- Report remote.origin.url for working directory - different for each example -->
    <exec executable="git" outputproperty="git.config.origin.url" dir="${user.dir}" failifexecutionfails="true">
      <arg value="config"/>
      <arg value="remote.origin.url"/>
    </exec>
    <echo>git origin url for ${user.dir} is ${git.config.origin.url}</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
