<project name="JENKINS-70858" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
        <arg value="--prune"/>
      </args>
    </git>
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <!-- JENKINS-70858 reports sparse checkout does not work -->
    <fileset id="files.list" dir="." includes="C*"/>
    <property name="prop.files.list" refid="files.list"/>
    <echo>Files list for 'C*' are ${prop.files.list}</echo>
    <dirset id="dir.list" dir="." includes="C*"/>
    <property name="prop.dir.list" refid="dir.list"/>
    <echo>Dir list for 'C*' are ${prop.dir.list}</echo>
    <dirset id="tdir.list" dir="." includes="T*"/>
    <property name="prop.tdir.list" refid="tdir.list"/>
    <echo>Dir list for 'T*' are ${prop.tdir.list}</echo>
    <git command="remote">
      <args>
        <arg value="-v"/>
      </args>
    </git>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
