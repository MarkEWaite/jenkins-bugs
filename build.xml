<project name="JENKINS-47496" default="increment" basedir=".">

  <property name="all.arg" value="--all"/>

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] build++, was ${build.number}"/>
      </args>
    </git>
    <!-- Tag predecessor commit with previous build number -->
    <git command="tag">
      <args>
        <arg value="${ant.project.name}-1.0.${build.number}"/>
        <arg value="HEAD^"/>
      </args>
    </git>
    <git command="push">
      <args>
        <arg value="origin"/>
        <arg value="${all.arg}"/>
      </args>
    </git>
    <git command="push">
      <args>
        <arg value="cache"/>
        <arg value="${all.arg}"/>
      </args>
    </git>
    <git command="push">
      <args>
        <arg value="bare"/>
        <arg value="${all.arg}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="tag">
      <args>
        <arg value="--contains"/>
        <arg value="HEAD^"/>
      </args>
    </git>
    <!-- JENKINS-47496 reports that new tags are not built. -->
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
