<project name="JENKINS-32174" default="increment-and-notify" basedir=".">

  <property environment="env"/>

  <!-- jenkins.url is hostname of Jenkins server to notify -->
  <condition property="jenkins.url" value="${env.JENKINS_URL}" else="http://mark-pc2.markwaite.net:8080/">
    <isset property="env.JENKINS_URL" />
  </condition>

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] build++, was ${build.number}"/>
        <arg value="-m"/>
        <arg value="Committed by ${user.name} from ${user.dir}, home is ${user.home}"/>
        <arg value="-m"/>
        <arg value="Java version is ${java.version} on ${os.name} with ${os.arch}"/>
      </args>
    </git>
    <!-- push to origin, intentionally do not push to cache or bare -->
    <git command="push" />
  </target>

  <target name="notify" description="Notify Jenkins of commit">
    <!-- Poke the notifyCommit URL with url and branches arguments -->
    <!-- Should start the build -->
    <!-- Must have curl program installed -->
    <exec executable="./notifyCommit" failonerror="true">
      <arg value="-s"/>
      <arg value="${jenkins.url}"/>
    </exec>
    <echo>notifyCommit script exited normally</echo>
    <echo>Sleeping for 2 minutes to allow notifyCommit to be processed</echo>
    <sleep minutes="2"/>
    <echo>Sleep completed</echo>
  </target>

  <target name="increment-and-notify" description="Increment build number and notify Jenkins" depends="increment,notify" />

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <!-- JENKINS-32174 reports that notifyCommit parameter branches is not honored
         if branch name contains slashes.  -->
    <echo>Displaying git log messages on this branch from last 15 minutes</echo>
    <git command="log">
      <args>
        <arg value="--since='15 minutes ago'"/>
      </args>
    </git>
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="3"/>
        <arg value="${env.GIT_COMMIT}"/>
      </args>
    </git>
    <echo>End of git log messages on this branch from last 15 minutes</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
