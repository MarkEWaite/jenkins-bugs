<project name="JENKINS-50168" default="increment" basedir=".">

  <property environment="env" />
  <!-- Give value in case env does not contain one -->
  <property name="env.JENKINS_URL" value="http://mark-pc2.markwaite.net:8080" />

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <!-- JENKINS-50168 reports that polling uses the last built branch
         in a parameterized Pipeline job.  -->
    <exec executable="curl" failonerror="true">
      <arg value="--fail" />
      <arg value="--silent" />
      <arg value="${env.JENKINS_URL}/job/Bugs-Individual/job/JENKINS-50168-parameterized-polls-with-last-built-branch/scmPollLog/" />
    </exec>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
