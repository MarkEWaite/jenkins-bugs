<project name="JENKINS-29603" default="increment" basedir=".">

  <!-- jenkins.hostname is hostname of Jenkins server to notify -->
  <property name="jenkins.hostname" value="jenkins.markwaite.net" />

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] build++, was ${build.number}"/>
      </args>
    </git>
    <git command="push" />
    <!-- intentionally do not push to cache or bare -->
    <!-- Poke the notifyCommit URL with a branchName argument -->
    <!-- Should be enough to start the build -->
    <exec executable="curl" failonerror="true">
      <arg value="-s" />
      <arg value="http://${jenkins.hostname}:8080/git/notifyCommit?url=git@github.com:MarkEWaite/jenkins-bugs.git&branchName=has-slash/JENKINS-29603" />
    </exec>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <!-- JENKINS-29603 reports that branches with slashes passed in branchName
         argumenbt of notifyCommit are ignored. -->
    <echo>Displaying git log messages on this branch from last 15 minutes</echo>
    <git command="log">
      <args>
        <arg value="--since='15 minutes ago'"/>
      </args>
    </git>
    <echo>End of git log messages on this branch from last 15 minutes</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
