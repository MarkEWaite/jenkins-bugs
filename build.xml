<project name="JENKINS-51638" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull">
      <args>
        <arg value="--all"/>
      </args>
    </git>
    <exec executable="git" outputproperty="git.branch" failifexecutionfails="false">
      <arg line="rev-parse --abbrev-ref HEAD"/>
    </exec>
    <echo message="Current branch: ${git.branch}"/> 
    <git command="merge">
      <args>
        <arg value="--no-edit"/>
        <arg value="origin/JENKINS-51638-project-1"/>
      </args>
    </git>
    <buildnumber file="${git.branch}/build.number" />
    <git command="add">
      <args>
        <arg value="${git.branch}/build.number"/>
      </args>
    </git>
    <exec executable="git" outputproperty="git.log.output">
      <arg value="log"/>
      <arg value="--oneline"/>
      <arg value="--numstat"/>
      <arg value="origin/${git.branch}..HEAD"/>
    </exec>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${git.branch}/build++, was ${build.number}"/>
        <arg value="-m"/>
        <arg value="Numstat output from git log --oneline"/>
        <arg value="-m"/>
        <arg value="Used for test assertions"/>
        <arg value="-m"/>
        <arg value="${git.log.output}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch" />
    <!-- JENKINS-51638 reports that Jenkinsfile reference to merge strategy 'default'
         fails on upgrade to git plugin 3.9.x -->
    <echo>Displaying git log messages on this branch from last 3 commits</echo>
    <git command="log">
      <args>
        <arg value="--graph"/>
        <arg value="--decorate"/>
        <arg value="-n"/>
        <arg value="3"/>
      </args>
    </git>
    <echo>End of git log messages on this branch from last 3 commits</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
