<project name="JENKINS-70810" default="increment" basedir=".">

<!-- Submitter reports that archiveArtifacts fails from Windows agents sometimes -->

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <property environment="env"/>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <echo>GIT_COMMIT is ${env.GIT_COMMIT}</echo>
    <echo>GIT_COMMITTER_NAME is ${env.GIT_COMMITTER_NAME}</echo>
    <echo>GIT_AUTHOR_NAME is ${env.GIT_AUTHOR_NAME}</echo>
    <git command="branch" />
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
