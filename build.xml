<project name="jenkins-bugs-master-branch" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <echo>java is ${java.version}</echo>
    <buildnumber/>
    <echo>Numero di compilazione era ${build.number}</echo>
    <exec executable="git">
      <arg value="add"/>
      <arg value="build.number"/>
      <arg value="."/>
    </exec>
    <exec executable="git">
      <arg value="commit"/>
      <arg value="-m"/>
      <arg value="${user.name} incremented build number. It was ${build.number} before"/>
      <arg value="build.number"/>
    </exec>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>

    <loadfile property="jenkins-bugs-README" srcFile="jenkins-bugs/README.md">
      <filterchain>
        <linecontains>
          <contains value="# "/>
        </linecontains>
      </filterchain>
    </loadfile>
    <echo/>
    <echo>==== REM contents of jenkins-bugs/README.md ====</echo>
    <echo>${jenkins-bugs-README}</echo>

    <echo/>
    <git command="branch" />

    <echo/>
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="2"/>
      </args>
    </git>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
