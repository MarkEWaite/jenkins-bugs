<project name="JENKINS-41906" default="increment" basedir=".">

  <property environment="env"/>
  <condition property="changeLogCount" value="${env.CHANGESET_SIZE}" else="0">
    <isset property="env.CHANGESET_SIZE"/>
  </condition>

  <tstamp>
    <format property="current.date" pattern="EEE, d MMM yyyy HH:mm:ss Z"/>
  </tstamp>

  <!-- Echo commit messages from changeset -->
  <target name="info" description="Commit messages from changeset">
    <echo>Date is ${current.date}</echo>
    <git command="branch"/>
    <echo>Displaying ${changeLogCount} git log messages in changeset for this build - CHANGESET_SIZE=${env.CHANGESET_SIZE}</echo>
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="${changeLogCount}"/>
      </args>
    </git>
    <echo>End of ${changeLogCount} git log messages in changeset for this build</echo>
  </target>

  <target name="increment" description="Increment build number">
    <echo>java is ${java.version}</echo>
    <exec executable="git">
      <arg value="pull"/>
    </exec>
    <buildnumber/>
    <exec executable="git">
      <arg value="add"/>
      <arg value="build.number"/>
    </exec>
    <exec executable="git">
      <arg value="commit"/>
      <arg value="-m"/>
      <arg value="${ant.project.name} ${user.name} build++. Was ${build.number}"/>
    </exec>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
