<project name="JENKINS-50401" default="increment" basedir=".">

  <target name="increment" description="Increment build number">
    <git command="pull" />
    <buildnumber/>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="[${ant.project.name}] ${user.name} build++, was ${build.number}"/>
      </args>
    </git>
  </target>

  <!-- Info about this repository -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <!-- JENKINS-50401 reports that checkout selects the wrong commit
         when the LocalBranch extension is used -->
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="1"/>
        <arg value="--pretty=format:HEAD-commit-SHA1-is-%H"/>
        <arg value="HEAD"/>
      </args>
    </git>
    <git command="log">
      <args>
        <arg value="-n"/>
        <arg value="1"/>
        <arg value="--pretty=format:remote-commit-SHA1-is-%H"/>
        <arg value="refs/remotes/origin/has-slash/${ant.project.name}"/>
      </args>
    </git>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
