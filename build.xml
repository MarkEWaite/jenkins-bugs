<project name="JENKINS-37727" default="push-new-branch" basedir=".">

  <property name="branches-file" value="branch-list.txt"/>

  <!-- Push a new branch, remove old branches -->
  <target name="push-new-branch" description="Push a new branch">
    <echo>java is ${java.version}</echo>
    <delete file="${branches-file}" quiet="true" />
    <git command="checkout">
      <args>
        <arg value="JENKINS-37727"/>
      </args>
    </git>
    <git command="pull" />
    <buildnumber/>
    <echo>Build number was ${build.number}</echo>
    <git command="add">
      <args>
        <arg value="build.number"/>
      </args>
    </git>
    <git command="commit">
      <args>
        <arg value="-m"/>
        <arg value="${user.name} updated build number, was ${build.number}"/>
      </args>
    </git>
    <git command="push"/>
    <exec executable="bash" dir=".">
      <arg value="remove-branches" />
    </exec>
    <git command="checkout">
      <args>
        <arg value="-b"/>
        <arg value="has-slash/JENKINS-37727-${build.number}"/>
      </args>
    </git>
    <git command="push">
      <args>
        <arg value="origin"/>
        <arg value="--set-upstream"/>
        <arg value="has-slash/JENKINS-37727-${build.number}"/>
      </args>
    </git>
    <git command="checkout">
      <args>
        <arg value="JENKINS-37727"/>
      </args>
    </git>
  </target>

  <!-- Information -->
  <target name="info" description="Report info about this repo">
    <echo>java is ${java.version}</echo>
    <echo>user dir is ${user.dir}</echo>
    <git command="branch">
      <args>
        <arg value="-r"/>
      </args>
    </git>
    <exec executable="git" output="${branches-file}">
      <arg value="branch" />
      <arg value="-r" />
    </exec>
    <resourcecount property="branch.count">
      <tokens>
        <concat>
          <filterchain>
            <linecontains>
              <contains value="origin/has-slash/JENKINS-37727-"/>
            </linecontains>
          </filterchain>
          <fileset file="${branches-file}"/>
        </concat>
      </tokens>
    </resourcecount>
    <echo>The file '${branches-file}' counts ${branch.count} branch.</echo>
  </target>

  <!-- From https://gist.github.com/davejlong/874521 -->
  <macrodef name="git">
    <attribute name="command" />
    <attribute name="dir" default="" />
    <element name="args" optional="true" />
    <sequential>
      <echo message="git @{command}" />
      <exec executable="git" dir="@{dir}">
        <arg value="@{command}" />
        <args/>
      </exec>
    </sequential>
  </macrodef>

</project>
